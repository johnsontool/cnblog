### task description:

copy the .md files in /nextjs/blog to /source/_posts/, and copy the images referenced by the just-copied .md files from /nextjs/static/images/ to /source/img/, by the the way, also update the  image path in the just-copied .md files as the referenced images moving to /source/img/

